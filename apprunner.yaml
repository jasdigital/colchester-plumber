version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using Dockerfile for build"
run:
  runtime-version: latest
  command: nginx -g 'daemon off;'
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production