version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Using Dockerfile for build"
run:
  command: nginx -g 'daemon off;'
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production