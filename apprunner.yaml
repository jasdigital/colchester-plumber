version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - yarn install --frozen-lockfile
      - yarn build
run:
  runtime-version: 22.20.0
  command: npx serve -s dist -l 8080
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production