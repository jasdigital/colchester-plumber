version: 1.0
build:
  commands:
    build:
      - echo "Build handled by Docker"
run:
  runtime: docker
  command: nginx -g 'daemon off;'
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production